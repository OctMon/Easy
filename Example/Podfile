use_frameworks!

target 'Easy_Example' do
  pod 'Easy', :path => '../'
  pod 'Easy/CommonLib', :path => '../'

  target 'Easy_Tests' do
    inherit! :search_paths
  end
end

target 'Easy_Example_Beta' do
    pod 'Easy', :path => '../'
    pod 'Easy/CommonLib', :path => '../'
    pod 'Easy/Beta', :path => '../'
end
